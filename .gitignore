.env
uploads/*
!uploads/.gitkeep
node_modules/
data/
recruitment-applications.jsonl
posts.jsonl
*.jsonl.migrated
public/api/voices.json.bak.*
public/api/voices/*.bak.*

# runtime logs (v0.7.0)
logs/

# vault: arquivos-fonte binarios ficam no Drive (inputs) — git versiona so o .md de contexto
vault/**/*.pdf
vault/**/*.xls
vault/**/*.xlsx
vault/**/*.docx
vault/**/*.pptx
vault/**/*.png
vault/**/*.jpg
vault/**/*.jpeg

# vault Obsidian acidental criada ao abrir a pasta (nao faz parte do projeto)
AA/
